Scripts for starting nanocubes from a csv file

Requires: Pandas and Numpy>=1.7

After compiling Nanocubes, run csv2Nanocube.py on the csv file and pipe the output to ncserve
  e.g. 
  python csv2Nanocube.py  --catcol='Primary Type' --timebinsize=1D crime50k.csv  | NANOCUBE_BIN=../src ../src/ncserve --rf=100000 --threads=100

  Example options:
    --catcol denotes column of categorical attribute   
    --timebinsize denotes the finest interval for aggregation overtime

Start a webserver in the web/
  e.g.
  cd ../web
  python -m SimpleHTTPServer 8000

Point your browser to http://localhost:8000/ to start exploring
